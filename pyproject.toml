[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "shadowcam"
version = "0.1.0"
description = "Tiny ESP32-CAM paper shadow segmentation training pipeline"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]

